Você é um desenvolvedor frontend sênior, especialista em CSS, dark mode e acessibilidade visual.

Estou desenvolvendo um sistema de gestão em JavaScript puro (localStorage) para a escola Bailado Carioca.
O sistema possui tema claro e tema escuro com toggle funcional.

⚠️ PROBLEMA ESPECÍFICO:
No MODO ESCURO, na aba “Alunos” → modal “Novo Aluno”, o campo SELECT “Unidade” apresenta baixo contraste:
- texto claro demais sobre fundo claro
- opções do dropdown ficam ilegíveis

Este problema NÃO ocorre no modo claro.

---

## ⚠️ REGRAS ABSOLUTAS
- NÃO alterar JavaScript
- NÃO alterar lógica
- NÃO alterar estrutura de dados
- NÃO alterar HTML funcional
- NÃO alterar comportamento do tema claro
- Resolver SOMENTE via CSS
- Corrigir APENAS o modo escuro

---

## 🎯 OBJETIVO
Ajustar o contraste do campo SELECT “Unidade” no modo escuro, garantindo:
- Texto legível no select fechado
- Texto legível nas opções do dropdown
- Contraste adequado para acessibilidade
- Coerência visual com o restante do dark mode
- Zero impacto no tema claro

---

## 🛠️ ESCOPO DA SOLUÇÃO
- Aplicar estilos apenas sob seletor de tema escuro (ex: `[data-theme="dark"]`, `.dark`, ou equivalente já existente)
- Ajustar:
  - `color`
  - `background-color`
  - `border-color` (se necessário)
- Estilizar também:
  - `select option`
- Utilizar variáveis CSS do dark mode, se existirem
- Caso não existam, usar cores seguras com fallback
- Evitar `!important` salvo se houver conflito inevitável

---

## 📱 CRITÉRIOS DE ACEITE
A correção está correta se:
- O texto da unidade fica claramente legível no modo escuro
- As opções do dropdown aparecem legíveis ao abrir
- O modo claro permanece inalterado
- Outros campos do sistema NÃO são afetados

---

🚨 IMPORTANTE:
Este é um ajuste visual pontual e específico do dark mode.
Não refatore estilos globais.
Não altere tokens de cor já consolidados.
Aplique a correção apenas onde for necessário.

Implemente a correção de forma simples, segura e isolada.
